# Status Check

A basic test to tell if our services are up or down.

## What's it Do

We define some basic checks.  We then use Actions to fire off these checks. Actions writes a file that we serve in pages.
